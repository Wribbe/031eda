#include "cipher.h"

class Cipher() : {}

public:

static unsigned char Cipher::encode(unsigned char c)
  /* Encode a character and return its encoded form. */
{
  return c;
}

static unsigned char Cipher::decode(unsigned char c)
  /* Take a encoded character and return its decoded form. */
{
  return c;
}
